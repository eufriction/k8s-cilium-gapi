apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: selector-denied-route
  namespace: backend-b
  labels:
    app.kubernetes.io/part-of: demo
spec:
  parentRefs:
    - name: selector-ns-gateway
      namespace: gateway-system
      sectionName: http-selector
  hostnames:
    - selector-denied.example.test
  rules:
    - backendRefs:
        - name: api
          port: 80
