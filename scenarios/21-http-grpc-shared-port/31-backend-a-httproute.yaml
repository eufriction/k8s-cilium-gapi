apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backend-a-https-route
  namespace: backend-a
  labels:
    app.kubernetes.io/part-of: demo
spec:
  parentRefs:
    - name: shared-port-gateway
      namespace: gateway-system
      sectionName: https
  hostnames:
    - backend.example.test
  rules:
    - backendRefs:
        - name: api
          port: 80
