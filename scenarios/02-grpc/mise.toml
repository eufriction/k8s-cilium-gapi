[tasks.start]
extends = "scenario:start"
description = "Deploy the TLS gRPC Gateway API scenario"
depends = ["//:kind:load:apps", "//:cert-manager:install"]

[tasks.delete]
extends = "scenario:delete"

[tasks.verify]
description = "Verify the TLS gRPC Gateway API scenario"
run = "./verify.sh \"{{config_root}}\""
