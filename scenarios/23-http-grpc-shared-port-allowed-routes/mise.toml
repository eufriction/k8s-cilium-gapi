[tasks.start]
extends = "scenario:start"
depends = ["//:kind:load:apps", "//:cert-manager:install"]

[tasks.delete]
extends = "scenario:delete"

[tasks.verify]
description = "Verify the shared-port + allowedRoutes.kinds scenario"
run = "./verify.sh \"{{config_root}}\""
