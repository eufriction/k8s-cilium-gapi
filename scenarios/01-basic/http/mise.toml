[tasks.start]
extends = "scenario:start"
description = "Deploy the multi-namespace HTTP Gateway API scenario"
depends = ["//:kind:load:apps{% if usage.verbose %} --verbose{% endif %}"]

[tasks.verify]
extends = "scenario:verify"
description = "Verify the multi-namespace HTTP Gateway API scenario"

[tasks.delete]
extends = "scenario:delete"
