apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: https-tls-shared-port-gateway-certificate
  namespace: gateway-system
  labels:
    app.kubernetes.io/part-of: demo
spec:
  secretName: https-tls-shared-port-gateway-tls
  commonName: web.example.test
  dnsNames:
    - web.example.test
    - localhost
  ipAddresses:
    - 127.0.0.1
  issuerRef:
    kind: Issuer
    name: https-tls-shared-port-selfsigned
