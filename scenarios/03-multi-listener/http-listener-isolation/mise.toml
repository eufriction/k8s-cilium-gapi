[tasks.start]
extends = "scenario:start"
description = "Deploy the HTTP listener isolation scenario (4 listeners, hostname specificity)"
depends = ["//:kind:load:apps{% if usage.verbose %} --verbose{% endif %}"]

[tasks.verify]
extends = "scenario:verify"
description = "Verify HTTP listener isolation"

[tasks.delete]
extends = "scenario:delete"
