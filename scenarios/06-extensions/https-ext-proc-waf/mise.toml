[env]
SCENARIO_SKIP_VERSIONS = "1.19.1 1.19.3 1.19.4 1.19.5 1.20.0-pre.1 1.20.0-pre.2"

[tasks.start]
extends = "scenario:start"
description = "Deploy the HTTPS ext_proc WAF scenario (Coraza WAF via CiliumEnvoyExtProcFilter)"
depends = [
  "//:kind:load:apps{% if usage.verbose %} --verbose{% endif %}",
  "//:cert-manager:install",
]

[tasks.verify]
extends = "scenario:verify"
description = "Verify the HTTPS ext_proc WAF scenario"

[tasks.delete]
extends = "scenario:delete"
