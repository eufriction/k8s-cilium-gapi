[env]
SCENARIO_SKIP_VERSIONS = "1.19.1 1.19.3 1.19.4 1.19.5 1.19.6 1.20.0-pre.1 1.20.0-pre.2 1.20.0"

[tasks.start]
extends = "scenario:start"
description = "Deploy the HTTP ExternalAuth missing-backend scenario"
depends = ["//:kind:load:apps{% if usage.verbose %} --verbose{% endif %}"]

[tasks.verify]
extends = "scenario:verify"
description = "Verify HTTP ExternalAuth fails closed when its backend is missing"

[tasks.delete]
extends = "scenario:delete"
