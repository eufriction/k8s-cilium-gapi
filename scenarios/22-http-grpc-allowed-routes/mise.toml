[tasks.start]
extends = "scenario:start"
description = "Deploy the allowed-routes kind-restricted scenario"
depends = ["//:kind:load:apps", "//:cert-manager:install"]

[tasks.verify]
extends = "scenario:verify"
description = "Verify the allowed-routes kind-restricted scenario"

[tasks.delete]
extends = "scenario:delete"
