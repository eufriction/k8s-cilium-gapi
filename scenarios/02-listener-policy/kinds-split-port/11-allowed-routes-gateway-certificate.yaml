apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: allowed-routes-gateway-certificate
  namespace: gateway-system
spec:
  secretName: allowed-routes-gateway-tls
  commonName: https-a.example.test
  dnsNames:
    - "*.example.test"
    - https-a.example.test
    - https-b.example.test
    - grpc-a.example.test
    - grpc-b.example.test
    - localhost
  ipAddresses:
    - 127.0.0.1
  issuerRef:
    kind: Issuer
    name: allowed-routes-selfsigned
