[tasks.start]
extends = "scenario:start"
description = "Deploy the allowedRoutes.namespaces cross-namespace scenario"
depends = ["//:kind:load:apps{% if usage.verbose %} --verbose{% endif %}"]

[tasks.verify]
extends = "scenario:verify"
description = "Verify allowedRoutes.namespaces is enforced per-listener"

[tasks.delete]
extends = "scenario:delete"
